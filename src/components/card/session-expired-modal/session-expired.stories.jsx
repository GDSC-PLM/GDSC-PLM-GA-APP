import SessionExpired from "./session-expired.component";

export default {
  title: "Modal/Session Expired",
  component: SessionExpired,
};

const Template = (args) => <SessionExpired {...args} />;
export const SessionExpiredStory = Template.bind({});
